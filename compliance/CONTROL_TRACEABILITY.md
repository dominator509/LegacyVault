# Privacy and Security Control Traceability

Status: engineering evidence only. This document does not constitute counsel,
vendor, insurance, executive, or regulatory approval.

| Commitment or risk | Technical control | Current proof | Release evidence still required |
|---|---|---|---|
| Tenant isolation | Transaction-scoped PostgreSQL RLS context; authenticated user-to-membership resolution; the shared client selects only from account-scoped household summaries when browser state is absent, while server authorization and RLS remain authoritative | `apps/web/app/_lib/api-client.ts`; `tests/unit/web-api-client.test.ts`; `tests/integration/database-rls.test.ts`; `tests/integration/membership-identity.test.ts` | Authenticated multi-household browser proof, production database configuration, and penetration test |
| Account takeover | Better Auth server sessions, application-origin-bound passkey WebAuthn verification, TOTP with five-attempt lockout, single-use recovery codes, 30-minute password reset with session revocation, Argon2id fallback, active-session review/revocation, and rate limiting | `packages/auth/src/auth.ts`; `apps/web/app/sign-in`; `apps/web/app/two-factor`; `apps/web/app/recover`; `apps/web/app/security`; `tests/e2e/web-accessibility.test.ts`; `tests/integration/better-auth.test.ts`; `tests/unit/password-hashing.test.ts`; `tests/unit/authorization-policy.test.ts` | Physical-authenticator cross-browser evidence and Turnstile deployment |
| Vault confidentiality | AES-256-GCM envelope encryption with tenant, record, purpose, and version AAD; per-household wrapped DEKs; authorized category-filtered reads decrypt only after policy evaluation and return neither ciphertext nor storage or wrapped-key material | `tests/unit/cryptography.test.ts`; `tests/integration/local-crypto-config.test.ts`; `tests/integration/application-runtime.test.ts`; `tests/integration/vault-api.test.ts`; `tests/e2e/web-accessibility.test.ts`; `scripts/security-check.mjs` | Production KMS/HSM configuration, key-recovery drill, and production R2 encrypted-content retrieval proof |
| Unauthorized staff access | Platform administrators have no vault permission; support access requires category-limited, time-bounded owner approval | `packages/auth/src/policy.ts`; `tests/unit/authorization-policy.test.ts` | Persisted JIT grant workflow, revocation live-fire, staff access review |
| Emergency release abuse | Emergency recipients have no standing read access and may request only for their active membership; reasons are DLP-screened and envelope-encrypted; owner-authorized optimistic decisions enforce a bounded delay before explicit category release. The client shows a no-release review screen and requires a fresh TOTP before submission | `packages/auth/src/policy.ts`; `apps/web/app/emergency-access`; `tests/unit/authorization-policy.test.ts`; `tests/integration/emergency-access.test.ts`; `tests/integration/vault-api.test.ts`; `tests/integration/bounded-access.test.ts`; `scripts/security-check.mjs` | Fraud-review operations and staged-pilot evidence |
| Document malware | Quarantine-first object storage and fail-closed ClamAV scan | `tests/integration/document-quarantine.test.ts`; `tests/integration/clamav.test.ts` | Production scanner capacity and failure drill |
| Document consent and minimization | Every upload atomically records a document-specific policy version and explicit original-retention choice; requested deletion occurs as a retryable workflow step only after the encrypted searchable derivative is stored. The first-party web server encrypts with the established envelope boundary and clears all transient plaintext, key, and ciphertext buffers before returning | `drizzle/0015_document_privacy_controls.sql`; `apps/web/app/api/documents/upload/route.ts`; `apps/web/app/vault/document-uploader.tsx`; `tests/integration/application-runtime.test.ts`; `tests/integration/vault-api.test.ts`; `scripts/security-check.mjs` | Counsel-approved document notice, production upload capacity, and production R2 deletion drill |
| Document content retrieval | All-category authorized retrieval requires a tenant-scoped clean retained row, clean object tag, ciphertext checksum match, and server-mediated envelope decryption; responses are no-store/nosniff attachments and never expose keys, wrapped material, or object locations | `packages/database/src/repository.ts`; `apps/api/src/runtime.ts`; `apps/api/src/routes/vault.ts`; `tests/integration/application-runtime.test.ts`; `tests/integration/vault-api.test.ts` | Production R2 retrieval probe and large-object streaming capacity proof |
| AI disclosure and prompt injection | Purpose-specific self-only opt-in, non-cacheable active-consent state and versioned withdrawal, DLP/secret detection, minimized canonical prompts, schema validation, fail-closed provider state, tenant/consent/prompt-isolated envelope-encrypted Redis exact caching, and required hashed-scope purge before active privacy deletion | `apps/api/src/routes/vault.ts`; `apps/web/app/ai-settings`; `tests/unit/ai-policy-gateway.test.ts`; `tests/integration/deepseek-contract.test.ts`; `tests/integration/ai-exact-cache.test.ts`; `tests/integration/privacy-deletion.test.ts`; `tests/integration/vault-api.test.ts`; `tests/live-fire/ai-interview-runtime.test.ts`; `scripts/security-check.mjs` | Current vendor contract/policy archive, approved policy version, counsel approval, authenticated browser withdrawal proof, production TLS Redis, and measured cache targets |
| AI does not become truth | AI output remains candidate data; a user confirmation transition is required | `tests/unit/domain-invariants.test.ts`; `tests/integration/vault-api.test.ts`; `tests/live-fire/ai-interview-runtime.test.ts`; `tests/e2e/web-accessibility.test.ts` | Production pilot content-review evidence |
| Fact decision integrity | Confirmation, rejection, and dispute use the persisted field category for authorization, idempotency keys, optimistic versions, and constrained status transitions; confirmed facts cannot be silently rejected | `packages/database/src/repository.ts`; `apps/api/src/routes/vault.ts`; `tests/integration/vault-api.test.ts`; `tests/unit/domain-invariants.test.ts`; `tests/e2e/web-accessibility.test.ts` | Browser dispute/correction proof and production pilot evidence |
| Customer audit access | Owner-authorized pages are returned only after full tenant-chain verification in a repeatable-read snapshot; actors remain pseudonymized and tenant identifiers are omitted | `packages/audit/src/store.ts`; `tests/integration/audit-store.test.ts`; `tests/integration/vault-api.test.ts`; `tests/unit/audit-chain.test.ts` | Export-format UX and production integrity-alert drill |
| Billing state integrity | Signed ordered Stripe events persist authoritative subscription, scheduled-cancellation, period, trial, and refund state; status-derived entitlements fail closed, past-due continuity is bounded and disables AI spend, unapproved numeric limits remain explicitly unset, customer reads omit provider identifiers, and no checkout or refund request fabricates payment success. The client renders only persisted state and accepts redirects only to Stripe-hosted HTTPS checkout or billing domains | `drizzle/0018_billing_lifecycle.sql`; `packages/domain/src/billing.ts`; `apps/api/src/adapters/stripe.ts`; `packages/database/src/repository.ts`; `apps/web/app/billing`; `tests/unit/billing-entitlements.test.ts`; `tests/integration/stripe-contract.test.ts`; `tests/integration/billing-webhook.test.ts`; `tests/integration/vault-api.test.ts` | Business-approved pricing/trial/refund/cancellation/quota terms and authenticated Stripe sandbox checkout, portal, webhook, and refund delivery |
| Household and member onboarding | A verified account creates its first household without a pre-existing tenant identity; one transaction persists the organization, household, wrapped household key, encrypted owner person, Owner membership, and account idempotency evidence. The account-scoped household summary exposes only the caller's membership-linked person identifier so self-scoped consent and privacy clients never require manual internal-ID transcription. Member invitations store only HMAC email identifiers and token hashes, expire after 72 hours, use bounded roles and optimistic versions, deliver through transactional email, and accept atomically only for the invited verified email. Owner-authorized administration can revoke only unused invitations and change only non-owner memberships to bounded household roles, with optimistic versions, idempotent replay, and household-version advancement | `drizzle/0016_household_onboarding.sql`; `packages/auth/src/identity.ts`; `packages/database/src/repository.ts`; `apps/api/src/routes/households.ts`; `apps/api/src/runtime.ts`; `apps/web/app/onboarding`; `apps/web/app/members`; `tests/unit/account-identity.test.ts`; `tests/integration/vault-api.test.ts`; `tests/integration/application-runtime.test.ts`; `tests/e2e/web-accessibility.test.ts` | Production Resend delivery |
| API contract inventory | A pinned Fastify-compatible generator publishes OpenAPI 3.1 from the live route registry, hides the document route from itself, disables caching, and verifies canonical implemented paths over real HTTP | `apps/api/src/server.ts`; `tests/e2e/health.test.ts`; `tests/integration/vault-api.test.ts`; `scripts/contract-test.sh` | Authenticated stateful Schemathesis fixtures for the currently reported authorization and missing-data warnings |
| Billing replay or spoofing | Raw-body signature verification, signed tenant metadata, idempotent ordered subscription and refund persistence, bounded provider retries with stable idempotency keys | `tests/unit/stripe-signature.test.ts`; `tests/integration/stripe-contract.test.ts`; `tests/integration/billing-webhook.test.ts` | Authenticated Stripe sandbox checkout, portal, subscription webhook, and refund webhook delivery |
| Privacy requests | Authenticated self-submission for access, correction, export, deletion, and appeal atomically creates replay-safe request and workflow evidence; a non-cacheable bounded subject-only ledger exposes status, while deletion confirmation/cancellation remain optimistic and self-scoped. The client resolves the active account membership instead of asking for an internal person ID and provides a destructive review screen, exact `DELETE` confirmation, and fresh TOTP before confirmation | `packages/database/src/repository.ts`; `apps/api/src/routes/vault.ts`; `apps/web/app/privacy`; `apps/web/app/_lib/api-client.ts`; `tests/integration/vault-api.test.ts`; `tests/integration/workflow-persistence.test.ts`; `tests/integration/privacy-deletion.test.ts`; `tests/e2e/web-accessibility.test.ts` | Approved identity-verification and jurisdiction timing policy, correction execution, appeal adjudication, shared-data disposition, processor deletion, notice delivery, and final completion |
| Reports and annual review | Idempotent requests create queued workflows; the worker decrypts tenant data transiently, labels non-authoritative facts, detects stale/contradictory/missing/expiring records deterministically, and persists only an envelope-encrypted payload; authorized no-store retrieval validates tenant and payload binding before returning plaintext | `tests/integration/application-runtime.test.ts`; `tests/integration/annual-review.test.ts`; `tests/integration/vault-api.test.ts`; `scripts/security-check.mjs` | Legacy report-column backfill/contract migration, notification delivery, and production worker capacity proof |
| Portable export delivery | Full-household export authorization, tenant RLS, encrypted signed archives, safe status metadata, and a five-minute object-store signed download that never exposes the internal object key. The browser creates the independent 256-bit export key, supports separate one-time key-file storage, and clears retained key bytes | `apps/web/app/exports`; `tests/integration/portable-export-workflow.test.ts`; `tests/integration/application-runtime.test.ts`; `tests/integration/vault-api.test.ts`; `tests/unit/portable-export.test.ts`; `tests/e2e/web-accessibility.test.ts` | Production R2 signed-download probe and restore/import rehearsal |
| Audit tampering and data leakage | Content-free canonical HMAC chain and database update/delete prevention | `tests/unit/audit-chain.test.ts`; `tests/integration/audit-store.test.ts` | Production key custody, alerting, and chain-verification drill |
| Export integrity | Ed25519 signing primitive uses PKCS8 key material | `tests/unit/cryptography.test.ts`; `tests/integration/local-crypto-config.test.ts` | Complete export assembly, verification tool, restore/import drill |
| Transactional email | Shared production Resend adapter with bounded retries, timeouts, rate-limit handling, idempotency, and header-injection defense; annual-review worker delivery requires verified membership and active consent, persists RLS evidence, and uses real local SMTP capture | `tests/unit/email-adapter.test.ts`; `tests/integration/email-capture.test.ts`; `tests/integration/notification-workflow.test.ts`; `tests/integration/queue-worker.test.ts` | Authenticated Resend delivery, suppression, bounce, and verified-domain proof |
| Retention and deletion | Category schedule, configured recovery/backup periods, delayed account-access removal, retained-evidence register, and fail-closed legal holds | `DATA_RETENTION_SCHEDULE.md`; `tests/integration/privacy-deletion.test.ts` | Approved periods, shared household-data decision, processor confirmations, backup-expiry live proof |
| Accessible client | Semantic landmarks, skip navigation, visible focus, 18 px default text, 44 px interactive targets, non-color status labels, plain-language live errors, printable layouts, and reduced-motion behavior are enforced across the real Next.js entry point and authenticated core task flow | `apps/web/app/layout.tsx`; `apps/web/app/styles.css`; `tests/e2e/web-accessibility.test.ts` | Manual screen-reader review, 200 percent zoom/reflow proof, and production browser-performance evidence |
| Launch-topology performance | The pinned official k6 2.0.0 container creates a verified Better Auth account through local SMTP, opens a server-side session, creates a household, and runs 250 authenticated household-list iterations from 50 concurrent VUs; the gate requires zero failed checks or requests and p95 below 400 ms. A real DeepSeek result is warmed once, encrypted in the tenant-isolated exact cache, then served through 50 concurrent runtime calls below 400 ms without additional provider payloads | `scripts/live-fire.sh`; `tests/performance/authenticated-api.js`; `tests/live-fire/ai-interview-runtime.test.ts`; `DECISIONS.md` ADR-071 | Production network/load evidence, dashboard Core Web Vitals below 3 seconds, upload acknowledgement below 2 seconds under load, 10 concurrent document jobs, and sustained 1,000-profile topology evidence |

The absence of a release-evidence item is a production blocker when the
corresponding source policy or `PRODUCTION_READINESS.md` requires it.
